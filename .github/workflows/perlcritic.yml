name: Perl Critic

on:
  push:
    branches:
      - master
      - hangy-patch-1
      - harsh-critic-annotated
      - harsh-critic
  pull_request:
    branches:
      - master
      - hangy-patch-1
      - harsh-critic-annotated
      - harsh-critic

jobs:
  perlcritic:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Perl Critic
      uses: hangy/action-perlcritic@15e0086dd664aca0094e4f9e866aa88960a4d97b
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        args: cgi/*.pl scripts/*.pl lib/ProductOpener/*.pm lib/*.pl t/*.t
