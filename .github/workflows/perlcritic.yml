name: Perl Critic

on:
  push:
    branches:
      - master
      - hangy-patch-1
      - harsh-critic-annotated
      - harsh-critic
  pull_request:
    branches:
      - master
      - hangy-patch-1
      - harsh-critic-annotated
      - harsh-critic

jobs:
  perlcritic:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Perl Critic
      uses: hangy/action-perlcritic@d9b5deed1018706050d7158c432186ed491d3e4e
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        args: cgi/*.pl scripts/*.pl lib/ProductOpener/*.pm lib/*.pl t/*.t
